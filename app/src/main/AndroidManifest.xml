<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.ferdyrodriguez.loquiero">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
            android:name=".App"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".usecases.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".usecases.loginOrRegistration.LoginOrRegistrationActivity"/>
        <activity android:name=".usecases.login.LoginActivity"/>
        <activity android:name=".usecases.registration.RegistrationActivity"/>
        <activity android:name=".usecases.main.MainActivity"/>
        <activity android:name=".usecases.userProducts.UserProductsActivity"/>
        <activity android:name=".usecases.addproduct.AddProductActivity"/>
    </application>

</manifest>